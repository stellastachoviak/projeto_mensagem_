# .env.example - Arquivo de exemplo de variáveis de ambiente
# Copie este arquivo para .env e ajuste os valores

# Configurações do Servidor
PORT=3000
NODE_ENV=development

# Configurações do Supabase
# Obtenha essas informações em: https://app.supabase.com/project/YOUR_PROJECT/settings/api
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sua_service_role_key_aqui

# IMPORTANTE: Use a Service Role Key (não a anon key) para operações do backend
# A Service Role Key bypassa o Row Level Security (RLS)

# JWT Secret (gere uma chave aleatória e segura)
JWT_SECRET=sua_chave_secreta_super_segura_aqui_mude_em_producao

# Configurações do Redis (para escala futura com Socket.IO)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Configurações do Firebase Cloud Messaging (para notificações push)
# FCM_SERVER_KEY=sua_chave_do_firebase_aqui
# FCM_PROJECT_ID=seu_projeto_firebase

# Configurações de CORS
CORS_ORIGIN=*

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logs
LOG_LEVEL=info


# COMO OBTER AS CREDENCIAIS DO SUPABASE:
# ============================================
# 
# 1. Acesse https://app.supabase.com
# 2. Selecione ou crie seu projeto
# 3. Vá em Settings > API
# 4. Copie:
#    - Project URL → SUPABASE_URL
#    - service_role (secret) → SUPABASE_SERVICE_ROLE_KEY
# 
# ATENÇÃO: NUNCA compartilhe a service_role key publicamente!
# ============================================